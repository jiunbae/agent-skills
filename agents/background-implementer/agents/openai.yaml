interface:
  display_name: "Background Implementer"
  short_description: "Parallel multi-agent implementation workflow"
  default_prompt: "Use $implementing-in-background to implement independent tasks with bounded parallel agents."

policy:
  allow_implicit_invocation: false
