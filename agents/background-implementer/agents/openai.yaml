interface:
  display_name: "Background Implementer"
  short_description: "Bounded parallel implementation with verified integration"
  default_prompt: "Use $background-implementer to implement independent tasks with isolated bounded workers and integrate only diffs that pass verification."

policy:
  allow_implicit_invocation: false
