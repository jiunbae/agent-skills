interface:
  display_name: "RPF"
  short_description: "Run a pipelined multi-agent pointer convergence loop"
  default_prompt: "Use $rpf to run a cost-aware multi-agent review, plan, work, and feedback loop until the pointer goals converge."
policy:
  allow_implicit_invocation: false
