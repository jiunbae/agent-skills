interface:
  display_name: "Background Planner"
  short_description: "Bounded parallel planning with stance-steered synthesis"
  default_prompt: "Use $background-planner to produce independent bounded planning perspectives and synthesize one plan with explicit conflicts and open questions."

policy:
  allow_implicit_invocation: false
